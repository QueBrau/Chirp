{
  "name": "media_reconcile_unresolved_runs",
  "description": "Runbook row: Media reconciliation dry-run findings (MONITORING-RUNBOOK.md, Current-service signal plan). Counts app.jobs.media_reconcile's single stdout media_reconcile_aggregate JSON line (board c414) whose unresolved count is greater than zero - a stored posts.media_urls value has a shape resolve_object_names() does not recognize. This is a resolver-coverage gap, not a failure: it means teach the resolver a new url form, never delete anything on the strength of this signal alone, and c410's job-agnostic failure policy already covers a failed execution. Risk: the jsonPayload.unresolved>0 numeric comparison in this filter is not yet proven by a live create - chirps-17 creates this metric and validates the time series against a real scheduled execution before activating the referencing alert policy; no notification delivery is claimed here.",
  "filter": "resource.type=\"cloud_run_job\" AND resource.labels.job_name=\"chirp-media-reconcile\" AND jsonPayload.event=\"media_reconcile_aggregate\" AND jsonPayload.unresolved>0",
  "disabled": false,
  "metricDescriptor": {
    "metricKind": "DELTA",
    "valueType": "INT64",
    "unit": "1",
    "displayName": "chirp media_reconcile_unresolved_runs occurrences"
  }
}
