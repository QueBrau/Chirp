{
  "name": "media_reconcile_eligible_runs",
  "description": "Runbook row: Media reconciliation dry-run findings (MONITORING-RUNBOOK.md, Current-service signal plan). Counts app.jobs.media_reconcile's single stdout media_reconcile_aggregate JSON line (board c414) whose eligible count is greater than zero - a scheduled dry run found at least one posts/ object old enough and unreferenced enough to be a deletion candidate. This is expected, review-worthy output, not a failure: the proposed chirp-media-reconcile schedule is dry-run only, nothing is deleted from this signal or from the schedule, and c410's separate job-failure policy still requires live validation. Risk: the jsonPayload.eligible>0 numeric comparison in this filter is not yet proven by a live create - the deploying operator creates this metric and validates the time series against a real scheduled execution before activating the referencing alert policy; no notification delivery is claimed here.",
  "filter": "resource.type=\"cloud_run_job\" AND resource.labels.job_name=\"chirp-media-reconcile\" AND jsonPayload.event=\"media_reconcile_aggregate\" AND jsonPayload.eligible>0",
  "disabled": false,
  "metricDescriptor": {
    "metricKind": "DELTA",
    "valueType": "INT64",
    "unit": "1",
    "displayName": "chirp media_reconcile_eligible_runs occurrences"
  }
}
