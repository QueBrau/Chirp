#!/usr/bin/env bash
# c418: inspect exact endpoint literals; effective bindings need separate review.
set -euo pipefail
here="$(cd "$(dirname "$0")" && pwd)"
source "$here/lib/pick-python.sh"
python=$(chirp_pick_python compiled-endpoints) || exit $?
exec "$python" "$here/compiled_endpoints.py" "$@"
