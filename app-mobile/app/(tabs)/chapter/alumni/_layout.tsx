/** Alumni stack: directory + job board + post-job form. */

import { Stack } from "expo-router";

export default function AlumniLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
